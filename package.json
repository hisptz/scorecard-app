{
  "name": "root",
  "version": "2.6.1-beta.01",
  "description": "Interactive Scorecard Application",
  "license": "BSD-3-Clause",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [

    ]
  },
  "scripts": {
    "start:app": "concurrently \"yarn start:libs\"  \"yarn workspace app start\"",
    "start:app-proxy": "concurrently \"yarn start:libs\"  \"yarn workspace app start:proxy\"",
    "deploy:app": "yarn workspace app deploy",
    "test:open:app": "yarn workspace app cy:open",
    "test:capture:app": "yarn workspace app cy:capture",
    "test:stub:app": "yarn workspace app cy:stub",
    "start:widget": "yarn workspace widget start",
    "build:utils": "yarn workspace @scorecard/utils build",
    "build:constants": "yarn workspace @scorecard/constants build",
    "build:state": "yarn workspace @scorecard/state build",
    "build:components": "yarn workspace @scorecard/components build",
    "build:hooks": "yarn workspace @scorecard/hooks build",
    "build:models": "yarn workspace @scorecard/models build",
    "build:resources": "yarn workspace @scorecard/resources build",
    "build:services": "yarn workspace @scorecard/services build",
    "build:app": "yarn build:all && yarn workspace app build",
    "build:widget": "yarn build:all && yarn workspace widget build",
    "start:libs": "concurrently \"yarn workspace @scorecard/utils start\" \"yarn workspace @scorecard/constants start\" \"yarn workspace @scorecard/state start\" \"yarn workspace @scorecard/components start\" \"yarn workspace @scorecard/hooks start\" \"yarn workspace @scorecard/models start\" \"yarn workspace @scorecard/resources start\" \"yarn workspace @scorecard/services start\"",
    "build:all": "yarn build:utils && yarn build:constants && yarn build:state && yarn build:components && yarn build:hooks && yarn build:models && yarn build:resources && yarn build:services"
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "lerna": "^6.6.0"
  }
}
