{
  "name": "hisptz-scorecard-workspaces",
  "version": "2.6.0-hotfix.1",
  "description": "Interactive Scorecard Application",
  "license": "BSD-3-Clause",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "shared/*"
    ],
    "nohoist": [
      "**/cypress-multi-reporters/**"
    ]
  },
  "scripts": {
    "start:app": "concurrently \"yarn start:libs\"  \"yarn workspace app start\"",
    "start:app-proxy": "concurrently \"yarn start:libs\"  \"yarn workspace app start:proxy\"",
    "deploy:app": "yarn workspace app deploy",
    "test:open:app": "yarn workspace app cy:open",
    "test:capture:app": "yarn workspace app cy:capture",
    "test:stub:app": "yarn workspace app cy:stub",
    "start:widget": "yarn workspace widget start",
    "build:utils": "yarn workspace @hisptz/scorecard-utils build",
    "build:constants": "yarn workspace @hisptz/scorecard-constants build",
    "build:state": "yarn workspace @hisptz/scorecard-state build",
    "build:components": "yarn workspace @hisptz/scorecard-components build",
    "build:hooks": "yarn workspace @hisptz/scorecard-hooks build",
    "build:models": "yarn workspace @hisptz/scorecard-models build",
    "build:resources": "yarn workspace @hisptz/scorecard-resources build",
    "build:services": "yarn workspace @hisptz/scorecard-services build",
    "build:app": "yarn build:all && yarn workspace app build",
    "build:widget": "yarn build:all && yarn workspace widget build",
    "start:libs": "concurrently \"yarn workspace @hisptz/scorecard-utils start\" \"yarn workspace @hisptz/scorecard-constants start\" \"yarn workspace @hisptz/scorecard-state start\" \"yarn workspace @hisptz/scorecard-components start\" \"yarn workspace @hisptz/scorecard-hooks start\" \"yarn workspace @hisptz/scorecard-models start\" \"yarn workspace @hisptz/scorecard-resources start\" \"yarn workspace @hisptz/scorecard-services start\"",
    "build:all": "yarn build:utils && yarn build:constants && yarn build:state && yarn build:components && yarn build:hooks && yarn build:models && yarn build:resources && yarn build:services"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
